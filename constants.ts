/**
 * Application Constants
 * 
 * Note: Mock data has been removed. All data is now fetched from the API.
 * See api/ directory for API client implementations.
 */

// Default pricing constants (fallback values)
export const DEFAULT_PRICE_PER_KG = 500; // Nigerian Naira
export const DEFAULT_PRICE_PER_TON = 450000; // Nigerian Naira